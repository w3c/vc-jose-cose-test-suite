services:
  example-sdk:
    build: ./example-sdk
    volumes:
      - ../tests:/tests
  example-server:
    build: ./example-server
    volumes:
      - ../tests:/tests
  example-node:
    build: ./example-node
    volumes:
      - ../tests:/tests

