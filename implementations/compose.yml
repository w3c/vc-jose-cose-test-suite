services:
  block:
    build: ./block
    volumes:
      - ../tests:/tests
#  example-sdk:
#    build: ./example-sdk
#    volumes:
#      - ../tests:/tests
#  example-server:
#    build: ./example-server
#    volumes:
#      - ../tests:/tests
